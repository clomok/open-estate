# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.pytest_cache/

# Environment
.env

# Dataset environments - one per running instance. These hold passwords, keys
# and the identity of somebody's real estate, so only the template is tracked.
datasets/*.env
!datasets/*.env.example

# Database & Instance Folder
instance/
*.db
*.sqlite3

# Tailwind/Node (Future proofing)
node_modules/

# Personal Data Seed Script
scripts/seed.py